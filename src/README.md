# src package

本目录包含一些常见机器学习算法的简洁纯 NumPy 实现：

- `linear_regression.py`：线性回归（梯度下降）
- `logistic_regression.py`：二元逻辑回归（梯度下降）
- `knn.py`：K 最近邻（纯 NumPy 实现）
- `kmeans.py`：KMeans 聚类（纯 NumPy 实现）
- `example.py`：示例脚本，演示如何使用上述类

运行示例（在仓库根目录）：

```bash
python -m src.example
```
